# adaptive-consensus-networks
A collection of mini networks where each sees only a local patch of the image, and communication links between them grow and prune dynamically based on how much they need to coordinate to produce the correct global prediction.
